version: '3.8'

services:
  neo4j:
    image: quay.io/lib/neo4j:latest
    container_name: iroko-neo4j
    hostname: neo4j
    ports:
      - "7474:7474"  # Browser interface
      - "7687:7687"  # Bolt protocol
    volumes:
      - neo4j_data:/data:Z
      - neo4j_logs:/logs:Z
      - neo4j_plugins:/plugins:Z
    environment:
      - NEO4J_AUTH=neo4j/${NEO4J_PASSWORD}
      - NEO4J_dbms_default__database=${NEO4J_DATABASE}
      - NEO4J_ACCEPT_LICENSE_AGREEMENT=yes
      - NEO4J_apoc_export_file_enabled=true
      - NEO4J_apoc_import_file_enabled=true
      - NEO4J_apoc_import_file_use__neo4j__config=true
      - NEO4J_PLUGINS=["apoc","graph-data-science"]
    healthcheck:
      test: ["CMD", "cypher-shell", "-u", "neo4j", "-p", "${NEO4J_PASSWORD}", "RETURN 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - iroko-net
    restart: unless-stopped

  postgres:
    image: quay.io/lib/postgres:16-alpine
    container_name: iroko-postgres
    hostname: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data:Z
      - ./initdb:/docker-entrypoint-initdb.d:Z
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-iroko}
      - POSTGRES_USER=${POSTGRES_USER:-iroko_user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-iroko_password}
      - POSTGRES_HOST_AUTH_METHOD=trust
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-iroko_user} -d ${POSTGRES_DB:-iroko}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - iroko-net
    restart: unless-stopped

  # memgraph:
  #   image: docker.uclv.cu/memgraph/memgraph-mage:latest
  #   container_name: iroko-memgraph-mage
  #   ports:
  #     - "9687:7687" 
  #     - "9688:7444"
  #   command: ["--log-level=TRACE"]
  #   volumes:
  #     - mg_lib:/var/lib/memgraph:Z
  #     - mg_log:/var/log/memgraph:Z
  #   networks:
  #     - iroko-net
  #   restart: unless-stopped
 
  # lab:
  #   image: docker.uclv.cu/memgraph/lab:latest
  #   container_name: iroko-memgraph-lab
  #   ports:
  #     - "3001:3000"
  #   depends_on:
  #     - memgraph
  #   environment:
  #     - QUICK_CONNECT_MG_HOST=memgraph
  #     - QUICK_CONNECT_MG_PORT=9687
  #   networks:
  #     - iroko-net
  #   restart: unless-stopped


volumes:
  neo4j_data:
    driver: local
  neo4j_logs:
    driver: local
  neo4j_plugins:
    driver: local
  postgres_data:
    driver: local
  # mg_lib:
  #   driver: local
  # mg_log:
  #   driver: local
    
networks:
  iroko-net:
    driver: bridge
    attachable: true
    labels:
      - description="Isolated network for iroko-cris services"